# Core
- Static single-page web app at project root. Main app: `index.html` with inline CSS/JS.
- Content model v1 is split under `data/`: verbs, nouns, patterns, expressions, learning paths.
- Durable docs: `README.md`, `HANDOFF.md`, `docs/DATA_MODEL.md`.
- Read `mem:tech_stack` for runtime/tooling, `mem:conventions` for content/code style, `mem:suggested_commands` for local commands, and `mem:task_completion` before finishing tasks.