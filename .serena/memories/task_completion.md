# Task Completion
- For code/data changes run `python3 scripts/validate.py`.
- For JSON data changes, also parse JSON and validate cross-file IDs when practical.
- For frontend fetch changes, test through local HTTP server because direct `file://` may block `fetch()`.
- Do not commit or push unless the user explicitly asks.